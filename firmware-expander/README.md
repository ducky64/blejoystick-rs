This target requires Rust nightly for rv32ec compilation. See documentation at https://github.com/ch32-rs/ch32-hal/tree/main/examples/ch32v003

Set this directory to use nightly:

```
rustup override set nightly 
```

This may also need a pre-release version of probe-rs.
