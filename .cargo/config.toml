[target.'cfg(all(target_arch = "arm", target_os = "none"))']
# runner = "probe-rs run --chip nRF52840_xxAA --speed 16000"
runner = "rust-script ./.cargo/flash-runner.rs"

[build]
target = "thumbv7em-none-eabihf"

[env]
DEFMT_LOG = "info"
